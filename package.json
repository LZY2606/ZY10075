{
  "name": "sbom-policy-gate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic SBOM (CycloneDX/SPDX) comparison, bounded policy exceptions, and audit",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "reset": "node -e \"import('node:fs').then(fs=>{fs.rmSync('data/state',{recursive:true,force:true});fs.rmSync('data/scans',{recursive:true,force:true});fs.rmSync('data/exports',{recursive:true,force:true});console.log('state cleared')})\""
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
